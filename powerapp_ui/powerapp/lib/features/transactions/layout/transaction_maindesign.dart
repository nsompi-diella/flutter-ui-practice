import 'package:flutter/material.dart';

class TransactionMaindesign extends StatelessWidget {
  const TransactionMaindesign({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
